# Public — exposed to the bundle (must be prefixed EXPO_PUBLIC_*)
EXPO_PUBLIC_BACKEND_URL="http://localhost:8001"

# Optional — only needed for Expo's hosted tunneling. Leave commented for local dev.
# EXPO_TUNNEL_SUBDOMAIN=
# EXPO_PACKAGER_HOSTNAME=
# EXPO_PACKAGER_PROXY_URL=
